<!--ブックマーク機能、退会している場合カウントされない-->
<% if post.bookmarked_by?(current_user) %>
  <% bookmark = post.bookmarks.find_by(user_id: current_user.id) %>
  <%= link_to post_bookmark_path(post, bookmark), method: :delete, remote: true do %>
    <i class="fa-solid fa-bookmark count-item" aria-hidden="true" style="color: #6495ED;">
    <%= active_bookmarks_count(post) %> ブックマーク</i>
  <% end %>
<% else %>
  <%= link_to post_bookmarks_path(post), method: :post, remote: true do %>
    <i class="fa-regular fa-bookmark count-item" aria-hidden="true">
    <%= active_bookmarks_count(post) %> ブックマーク</i>
  <% end %>
<% end %>
